version: '3.8'

services:
  opengov-monitor:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "80:80"
    volumes:
      - data:/data
    restart: unless-stopped

volumes:
  data:
